<div class="container">
    <div id="carousel-example-generic" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
            <?php $h = 0; foreach ($this->hotIndex as $hots) { ?>
                <li data-target="#carousel-example-generic" data-slide-to="<?=$h++?>" class="<?=($h == 1) ? "active" : ""?>"></li>
            <?php } ?>
        </ol>
        <div class="carousel-inner" role="listbox">
            <?php $h = 0; foreach ($this->hotIndex as $hots_display) { ?>
                <?php  preg_match_all('/<img.*src=\"(.*)\".*>/iU', $hots_display['recipe'], $images);
                if (empty($images[1][0]) == false) {
                    $min = 0;
                    $max = count($images[1]) - 1;
                    $linkImage = $images[1][rand ($min ,$max)];
                }
                ?>
            <div class="navbar-default item <?=($h == 0) ? "active" : ""?>">
                <img data-src="holder.js/1140x300/auto/#11<?=$h?>:#11<?=$h?>/" alt="" style="max-width:1140px; max-height:500px; min-width:1140px; min-height:500px; overflow: hidden; background: url(<?=$linkImage?>) no-repeat 50% 50%;" data-holder-rendered="true">
                <div class="container navbar-default">
                    <div class="carousel-caption">
                        <h1><?=$hots_display['title']?></h1>
                        <p><a class="btn btn-lg btn-primary" href="read/<?=$this->replaceOnLink($hots_display['title'])?>" role="button">Przejdź do wpisu</a></p>
                    </div>
                </div>
            </div>
                <?php $h++ ?>
            <?php } ?>
        </div>
    </div>
</div>
<div class="container theme-showcase" role="main">
    <div class="row">
        <div class="col-sm-8 blog-main">
            <?php if ($this->resultRecipe != NULL){ $i = 0; ?>
                <?php foreach ($this->resultRecipe as $values) { ?>
                    <?php if($i%2 == 0) { ?>
                        <?=$this->partial('index/recipe/recipeIndexL.phtml', $values); ?>
                    <?php }else { ?>
                        <?=$this->partial('index/recipe/recipeIndexR.phtml', $values); ?>
                    <?php
                    }
                    if ($i == $i) {
                        echo '<hr class="featurette-divider">';
                    }
                    $i++;
                }
            }else{
            ?>
                <div class="alert alert-info" role="alert">
                    <p>Nie dodano jeszcze żandego przepisu do strony.</p>
                    <p>Zapraszamy później, bądź na naszego Facebook'a.</p>
                </div>
                <?=$this->render('facebook.phtml'); ?>
            <?php } ?>

        </div>
        <?=$this->render('sidebar.phtml'); ?>
    </div>
    <nav>
        <ul class="pager">
            <li class="previous disabled"><a href="#">&larr; Older</a></li>
            <li class="next"><a href="#">Newer &rarr;</a></li>
        </ul>
    </nav>

    <script>
    $(document).ready(function() {
        $('#editor').summernote(
            {height: 150}
        );
    });
    </script>
    <div class="modal fade bs-example-modal-lg" id="bs-example-modal-edit-post" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <form class="form-horizontal" role="form" id="form" method="post" action="<?=DEF_CONTROLER__LOGIN?>">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
		<div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
                    <h4 class="modal-title" id="myModalLabel">Modal title</h4>
                </div>
                <div class="modal-body">
                    <div id="alerts"></div>
                    <textarea id="editor" contenteditable="true" name="editor" style="overflow: auto"></textarea>
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
        </form>
    </div>
    <input type="button" class="btn glyphicon-edit" data-toggle="modal" data-target="#bs-example-modal-edit-post"/>
    <hr class="featurette-divider">
</div>
<?php
//$data = array("Biały ser", "Ciasta, Mak", "z dodatkiem alkoholu");
//var_dump(json_encode($data, JSON_UNESCAPED_UNICODE));
?>